version: "3.1"
services:

  stormy_apollo:
    build: b-apollo
    # ---------------
    # Comment these two lines out
    # after the first run, when the 
    # api keys are all set up.
    #stdin_open: true
    #tty: true
    # ---------------
    volumes:
      - "./b-apollo/bot/apikeys.py:/apollo/bot/apikeys.py"
      - "./b-apollo/bot/keys:/apollo/bot/keys"

  stormy_ginsberg:
    build: b-ginsberg
    # ---------------
    # Comment these two lines out
    # after the first run, when the 
    # api keys are all set up.
    #stdin_open: true
    #tty: true
    # ---------------
    volumes:
      - "./b-ginsberg/bot/apikeys.py:/ginsberg/bot/apikeys.py"
      - "./b-ginsberg/bot/keys:/ginsberg/bot/keys"

  stormy_milton:
    build: b-milton
    # ---------------
    # Comment these two lines out
    # after the first run, when the 
    # api keys are all set up.
    #stdin_open: true
    #tty: true
    # ---------------
    volumes:
      - "./b-milton/bot/apikeys.py:/milton/bot/apikeys.py"
      - "./b-milton/bot/keys:/milton/bot/keys"

  stormy_tripos:
    build: b-tripos
    # ---------------
    # Comment these two lines out
    # after the first run, when the 
    # api keys are all set up.
    #stdin_open: true
    #tty: true
    # ---------------
    volumes:
      - "./b-tripos/bot/apikeys.py:/tripos/bot/apikeys.py"
      - "./b-tripos/bot/keys:/tripos/bot/keys"
      - "./b-tripos/bot/latex:/tripos/bot/latex"
